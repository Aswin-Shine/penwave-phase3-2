apiVersion: v1
kind: ConfigMap
metadata:
  name: penwave-backend-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "penwave-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
